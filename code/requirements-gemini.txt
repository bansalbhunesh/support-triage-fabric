# Minimal install: Google Gemini synthesis only (no Anthropic SDK).
# Challenge does not require Anthropic; use this if you run with GOOGLE_API_KEY / GEMINI_API_KEY only.
# Python 3.10+ recommended (NumPy 2.x).
google-generativeai==0.8.6
pydantic==2.12.5
rank-bm25==0.2.2
numpy==2.4.3
